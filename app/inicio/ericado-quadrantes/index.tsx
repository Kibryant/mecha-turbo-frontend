import { SafeAreaView } from "react-native";
import { Text } from "react-native";

export default function EnriçadoQuadrantes() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>Enriçado Quadrantes</Text>
    </SafeAreaView>
  );
}
